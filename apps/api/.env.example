# API server
NODE_ENV=development
API_PORT=4000
GPS_PROVIDER=demo
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pasa_a_paso?schema=public
API_CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Supabase Auth (prepared, not active in FASE 1)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_JWT_ISSUER=https://your-project.supabase.co/auth/v1
SUPABASE_JWT_AUDIENCE=authenticated

# GPS/webhook security (prepared for later phases)
GPS_WEBHOOK_SHARED_SECRET=change-me
