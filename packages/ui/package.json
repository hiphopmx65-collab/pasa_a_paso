{
  "name": "@paso-a-paso/ui",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('No tests for @paso-a-paso/ui in FASE 1')\""
  }
}
